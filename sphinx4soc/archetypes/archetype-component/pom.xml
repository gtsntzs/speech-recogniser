<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>soa.speech</groupId>
    <artifactId>archetypes</artifactId>
    <version>1.0.5</version>
  </parent>

  <groupId>soa.speech.archetypes</groupId>
  <artifactId>archetype-component</artifactId>
  <version>1.0.5</version>
  <packaging>maven-archetype</packaging>

  <name>Component :: Archetype</name>
  <description>
		Archetype for creating new shpinx4soc camel components.
  </description>

  <url>${site.url}/archetypes/${project.artifactId}</url>

  <distributionManagement>
    <site>
      <id>${site.deploy.id}</id>
      <!-- Note that GitHub does not like having leading slash, so make it explicit -->
      <url>${site.deploy.url}archetypes/${project.artifactId}</url>
    </site>
  </distributionManagement>
</project>
